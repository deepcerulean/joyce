source 'https://rubygems.org'

gemspec

# gem 'redis' # TODO add to metacosm dependencies?
gem 'gosu'
gem 'metacosm', require: 'metacosm/remote_simulation', path: '../metacosm'

group :development do
  gem 'kramdown'
  gem 'pry'
end
